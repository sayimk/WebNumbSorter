using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using NumberSorter.Models.DB;

namespace NumberSorter.Tests.Models.DB
{
    [TestClass]
    public class DBAccessModelTest
    {
       /* [TestMethod]
        public void addSortedToDB_ifAdded()
        {

            //need to figure out how to test static classes

            string testString = "";
            string testDirection = "";
            double testTime = 0.1233;

            Boolean isSuccess = false;


            isSuccess = DBAccessModel.addSortedToDB(testString, testDirection, testTime);

            DBAccessModel.removeFromDB(testString, testDirection, testTime);

            Assert.IsTrue(isSuccess);
        }*/
    }
}
